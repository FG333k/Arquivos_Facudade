//Garante que o valor da variável seja sempre lido da memória principal (evita caching em threads).

public class Volatile {
    /*
    public class Servidor {
        private volatile boolean ativo = true;

        public void deligar(){
            ativo = false;
        }
    }
     */
}
