//Classes: Não podem ser instanciadas (apenas herdadas).
//Métodos: Sem implementação (devem ser sobrescritos).

public class Abstract {
    /*
    abstract class Forma{       //Classe abstrata
        abstract double calcularArea();     //Método abstrato (sem corpo)
    }

    class Circulo extends Forma{
        double raio;

        @Override
        double calcularArea(){
            return Math.PI * raio * raio;
        }
    }

    // Uso:
    Forma forma = new Circulo();    // Correto    
    // Forma forma = new Forma();   // Erro: classe abstrata
     */
}
