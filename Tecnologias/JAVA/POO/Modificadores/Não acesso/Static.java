//Pertence à classe (não a instaâcias).

public class Static {
    /*
    public class Contador {
        static int total;       //Variavel de classe (compartilahada)

        public Contador(){
            total++;            //Incrementa o contador a cada nova instancia
        }

        public static void exibirTotal(){   //Método de classe
            System.out.println("Total: " + total);
        }
    }

    // Uso:
    new Contador();
    new Contador();
    Contador.exibirTotal();     //Saída: "Total: 2" (sem precisar de instancia)
     */
}
