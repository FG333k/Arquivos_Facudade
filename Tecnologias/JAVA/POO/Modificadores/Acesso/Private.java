//Acesso apenas na própria classe

public class Private {
    /*
    public class ContaBancaria {
        private double saldo;       //Atributo privado

        public void depositar(double valor){
            saldo += valor;         //Acesso permitido (dentro da classe )
        }

        public double getSaldo(){
            return saldo;           //Método público para acessar o saldo
        }
    }

    // Uso:
    ContaBancaria conta = new ContaBancaria();
    conta.depositar(1000);
    System.out.println(conta.getSaldo());   //Saída: 1000.0
    // conta.saldo = 500;    //Erro: saldo é privado
     */
}
